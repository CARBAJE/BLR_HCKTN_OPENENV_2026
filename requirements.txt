# ── FastAPI / server layer ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pyyaml>=6.0.1
python-dotenv>=1.0.1

# ── DOMAgent dependencies ───────────────────────────────────────────────────
# torch CPU wheels installed separately in Dockerfile (before this file)
torch-geometric>=2.3.0
sentence-transformers>=2.2.2
requests>=2.31.0
numpy>=1.24.0
